# ss_depth 개발 개요 및 개발 계획

## 1. 프로젝트 개요

`ss_depth`는 LiDAR와 RealSense D435 depth camera를 사용해서 이동형 구루마의 주변 환경을 시각화하는 ROS2 기반 센서 시각화 프로젝트이다.

이번 단계의 목표는 실제 로봇 제어가 아니다.
로봇에 바로 연결하지 않고, 바퀴 4개가 달린 구루마 위에 Jetson, LiDAR, RealSense D435를 올려두고 사용자가 직접 구루마를 이동시키면서 센서 결과를 모니터에 시각화한다.

모니터에는 두 개의 화면을 표시한다.

```text
1. LiDAR 기반 지도 화면
2. RealSense 기반 1인칭 카메라 화면
```

지도 화면은 구루마의 위치, 방향, 주변 지도, 목적지, 예상 이동 동선을 보여준다.
1인칭 카메라 화면은 전방 카메라 영상, 가까운 장애물 bbox, 장애물 거리, 회피 권장 방향을 보여준다.

---

## 2. 현재 개발 단계의 목적

현재 단계에서는 실제 주행 제어를 구현하지 않는다.

이번 단계의 목적은 다음과 같다.

```text
LiDAR와 depth camera를 사용해서
현재 위치, 주변 지도, 전방 장애물, 거리, 회피 권장 방향을
모니터에서 사람이 확인할 수 있게 만드는 것
```

즉, `ss_depth`는 먼저 **센서 시각화 대시보드**로 개발한다.

---

## 3. 전체 시스템 목표

최종적으로 만들 화면은 다음 구조를 가진다.

```text
┌──────────────────────────┬──────────────────────────┐
│                          │                          │
│     LiDAR 지도 화면       │    1인칭 카메라 화면      │
│                          │                          │
└──────────────────────────┴──────────────────────────┘
```

왼쪽 또는 첫 번째 화면:

```text
LiDAR 기반 지도 화면
```

오른쪽 또는 두 번째 화면:

```text
RealSense 기반 1인칭 카메라 화면
```

두 화면은 하나의 OpenCV 창 안에서 좌우로 붙여서 보여주거나, 추후 별도 GUI로 분리할 수 있다.
초기 구현은 단순성을 위해 OpenCV 기반 단일 대시보드 화면으로 진행한다.

---

## 4. 하드웨어 구성

사용 장비:

```text
Jetson Orin Nano
RealSense D435
2D LiDAR
바퀴 4개가 달린 수동 이동 구루마
모니터
키보드 / 마우스
```

동작 방식:

```text
1. Jetson, LiDAR, RealSense를 구루마 위에 장착한다.
2. 사용자가 직접 구루마를 밀거나 움직인다.
3. 센서는 이동 중 주변 환경을 인식한다.
4. 모니터에는 지도 화면과 1인칭 카메라 화면이 표시된다.
5. 실제 모터 제어 또는 로봇 자동 주행은 나중에 연결한다.
```

---

## 5. 센서별 역할

## 5.1 LiDAR 역할

LiDAR는 2D 지도와 위치 기반 시각화를 담당한다.

LiDAR 기반 화면에 필요한 정보:

```text
2D 지도
현재 구루마 위치
현재 구루마 방향
목적지 위치
목적지까지의 예상 이동 동선
지도상 장애물 위치
회피 권장 방향
```

LiDAR는 넓은 범위의 공간 구조를 파악하는 데 사용한다.

---

## 5.2 RealSense D435 역할

RealSense D435는 전방 1인칭 화면과 가까운 장애물 판단을 담당한다.

RealSense 기반 화면에 필요한 정보:

```text
전방 color camera 영상
aligned depth image
가까운 장애물 bbox
bbox까지의 거리
전방 5분할 영역 기반 회피 권장 방향
```

RealSense는 구루마 바로 앞의 가까운 장애물과 거리 판단에 사용한다.

---

## 6. 지도 화면 개발 개요

지도 화면은 LiDAR와 SLAM 결과를 기반으로 구성한다.

## 6.1 지도 기본 표시

지도 화면에는 다음 정보를 표시한다.

```text
LiDAR로 생성된 2D 지도
현재 구루마 위치
현재 구루마 방향
```

구루마는 지도 위에서 자동차 모양 아이콘으로 표시한다.

표시 기준:

```text
지도 방위는 고정한다.
구루마 위치는 지도 위에서 이동한다.
구루마 방향은 실제 이동 방향 또는 추정 yaw 값에 따라 회전한다.
```

---

## 6.2 구루마 표시

구루마는 지도 중앙 또는 현재 위치에 자동차 모양으로 표시한다.

자동차 아이콘은 방향성을 가져야 한다.

```text
자동차 앞부분이 현재 정면 방향을 가리킨다.
구루마가 회전하면 자동차 아이콘도 회전한다.
```

초기 구현에서는 복잡한 자동차 이미지를 사용하지 않고, 삼각형 또는 단순한 자동차 도형으로 표시해도 된다.

---

## 6.3 목적지 표시

사용자가 목적지를 설정하면 지도 위에 목적지를 표시한다.

초기 구현에서는 목적지 설정 방식을 단순화한다.

초기 목적지 입력 방식 후보:

```text
1. 코드에 고정 좌표로 목적지 설정
2. 마우스 클릭으로 목적지 설정
3. ROS2 topic 또는 service로 목적지 좌표 전달
```

초기 개발에서는 마우스 클릭 또는 고정 좌표 방식 중 하나로 시작한다.

---

## 6.4 예상 이동 동선 표시

목적지가 설정되면 현재 위치에서 목적지까지의 예상 이동 동선을 지도 위에 점선으로 표시한다.

경로는 지도상의 장애물을 고려해야 한다.

초기 구현 단계:

```text
1단계: 현재 위치와 목적지를 단순 직선 점선으로 연결
2단계: occupancy grid 기반으로 장애물을 피하는 경로 계산
3단계: A* 기반 경로를 점선으로 표시
```

처음부터 완전한 경로 계획을 구현하지 않는다.
먼저 지도 위에 목적지와 예상 경로를 시각적으로 표시하는 구조부터 만든다.

---

## 6.5 지도 화면 회피 권장 방향 표시

이동 중 장애물이 가까워지면 지도 화면에도 회피 권장 방향을 표시한다.

표시 방향:

```text
GO
TURN_LEFT
TURN_RIGHT
```

지도 화면에서는 구루마 주변 또는 지도 상단에 회피 권장 방향 화살표를 표시한다.

---

## 7. 1인칭 카메라 화면 개발 개요

1인칭 카메라 화면은 RealSense color image와 aligned depth image를 함께 사용한다.

## 7.1 기본 영상 표시

화면에는 RealSense color image를 표시한다.

사용 토픽:

```text
/camera/camera/color/image_raw
```

---

## 7.2 depth 기반 장애물 bbox 표시

YOLO 객체 인식이 아니라 aligned depth image에서 가까운 픽셀 덩어리를 찾아 bbox로 표시한다.

사용 토픽:

```text
/camera/camera/aligned_depth_to_color/image_raw
```

장애물 후보 기준:

```text
0.4m 이내의 가까운 depth 픽셀 덩어리
```

작은 픽셀 노이즈는 제거한다.

노이즈 제거 기준:

```text
depth == 0 제외
0.2m 미만 제외
1.2m 초과 제외
near pixel ratio 사용
connected component 사용
작은 blob 제거
연속 프레임 안정화
```

---

## 7.3 bbox 거리 표시

감지된 가까운 픽셀 덩어리에는 bbox를 표시한다.

bbox와 함께 거리값을 표시한다.

거리 계산 방식:

```text
bbox 내부 유효 depth 값의 median distance
```

표시 예시:

```text
0.38m
Obstacle 0.38m
```

---

## 7.4 5분할 방향 판단

1인칭 카메라 화면의 하단 60%를 사용해서 방향 판단을 한다.

분할 기준:

```text
FAR_LEFT | LEFT | CENTER | RIGHT | FAR_RIGHT
```

장애물 판단 기준:

```text
0.4m 이내
near pixel ratio >= 8%
largest blob area >= 500px
median distance <= 0.4m
```

최종 방향 상태:

```text
GO
TURN_LEFT
TURN_RIGHT
```

---

## 7.5 1인칭 화면 화살표 표시

카메라 화면에는 이동 권장 방향을 초록색 화살표로 표시한다.

표시 위치:

```text
GO: 화면 상단 중앙
TURN_LEFT: 화면 왼쪽 중앙
TURN_RIGHT: 화면 오른쪽 중앙
```

장애물이 사라지면 다시 `GO`를 표시한다.

---

## 8. LiDAR와 depth camera 연동 방향

LiDAR와 depth camera는 역할을 분리해서 사용한다.

```text
LiDAR:
지도 생성, 위치 추정, 방향 추정, 목적지 경로 표시

Depth camera:
전방 가까운 장애물 감지, bbox 표시, 거리 표시, 회피 방향 판단
```

초기 단계에서는 두 센서의 결과를 하나의 대시보드에 동시에 표시한다.

이후 단계에서 두 센서의 판단 결과를 통합한다.

통합 예시:

```text
LiDAR 경로상으로는 직진 가능
Depth camera 전방 0.4m 이내 장애물 감지
→ 화면에는 TURN_LEFT 또는 TURN_RIGHT 권장 방향 표시

LiDAR 지도상 장애물 회피 경로 존재
Depth camera도 전방 장애물 감지
→ 지도와 1인칭 화면 모두 같은 회피 방향 표시
```

---

## 9. ss_depth 프로젝트 구조

ROS2 workspace 기준으로 다음 구조를 사용한다.

```text
~/ss_robot_ws/src/ss_depth/
  package.xml
  setup.py
  setup.cfg
  resource/
    ss_depth
  ss_depth/
    __init__.py

    dashboard_node.py
    config.py
    types.py

    realsense_subscriber.py
    depth_obstacle_detector.py
    direction_decider.py
    camera_view_renderer.py

    lidar_map_subscriber.py
    map_view_renderer.py
    path_planner.py

    dashboard_renderer.py
  launch/
    dashboard.launch.py
  README.md
```

---

## 10. 파일별 역할

## 10.1 dashboard_node.py

전체 실행 노드이다.

역할:

```text
ROS2 Node 생성
RealSense color/depth 토픽 구독
LiDAR/SLAM map 토픽 구독
현재 pose 토픽 구독
depth 장애물 감지 호출
방향 판단 호출
지도 화면 렌더링 호출
카메라 화면 렌더링 호출
대시보드 화면 합성
OpenCV 화면 표시
```

초기에는 가능한 단순하게 작성한다.

---

## 10.2 config.py

프로젝트 파라미터를 관리한다.

포함할 값:

```python
COLOR_TOPIC = "/camera/camera/color/image_raw"
DEPTH_TOPIC = "/camera/camera/aligned_depth_to_color/image_raw"

MAP_TOPIC = "/map"
POSE_TOPIC = "/pose"

ROI_HEIGHT_RATIO = 0.6
SECTION_COUNT = 5

VALID_MIN_M = 0.2
VALID_MAX_M = 1.2
AVOID_DISTANCE_M = 0.4

NEAR_RATIO_THRESHOLD = 0.08
MIN_BLOB_AREA = 500
STABLE_FRAME_COUNT = 3

DASHBOARD_WINDOW_NAME = "ss_depth dashboard"
```

---

## 10.3 types.py

공통 데이터 구조를 정의한다.

포함할 데이터:

```text
Direction
SectionAnalysis
ObstacleBox
RobotPose
PathPoint
DashboardState
```

방향 상태:

```text
GO
TURN_LEFT
TURN_RIGHT
```

---

## 10.4 realsense_subscriber.py

RealSense color/depth 토픽 수신을 담당한다.

역할:

```text
sensor_msgs/Image 수신
CvBridge 변환
latest_color_image 저장
latest_depth_image 저장
```

---

## 10.5 depth_obstacle_detector.py

depth image 기반 장애물 후보를 감지한다.

역할:

```text
하단 ROI 추출
5분할 영역 분석
0.4m 이내 near mask 생성
connected component 계산
작은 blob 제거
ObstacleBox 생성
SectionAnalysis 생성
```

출력:

```text
obstacle_boxes
section_analyses
```

---

## 10.6 direction_decider.py

depth 분석 결과를 기반으로 이동 권장 방향을 결정한다.

역할:

```text
section_analyses 입력
GO / TURN_LEFT / TURN_RIGHT 판단
3프레임 안정화 적용
최종 direction 반환
```

---

## 10.7 camera_view_renderer.py

1인칭 카메라 화면을 렌더링한다.

역할:

```text
color image 표시
ObstacleBox bbox 표시
bbox 거리 표시
direction 화살표 표시
```

---

## 10.8 lidar_map_subscriber.py

LiDAR / SLAM 관련 토픽을 수신한다.

초기 수신 대상:

```text
/map
/scan
/pose 또는 /tf
```

초기에는 `/map`과 pose 수신을 중심으로 구현한다.

---

## 10.9 map_view_renderer.py

지도 화면을 렌더링한다.

역할:

```text
OccupancyGrid map 표시
구루마 현재 위치 표시
구루마 방향 표시
목적지 표시
예상 이동 동선 점선 표시
회피 권장 방향 표시
```

---

## 10.10 path_planner.py

목적지까지의 예상 이동 동선을 계산한다.

초기 구현:

```text
현재 위치와 목적지를 직선 점선으로 연결
```

추후 구현:

```text
OccupancyGrid 기반 A* 경로 계산
장애물을 고려한 경로 생성
```

---

## 10.11 dashboard_renderer.py

지도 화면과 1인칭 카메라 화면을 하나의 대시보드 이미지로 합성한다.

역할:

```text
map_view image 생성
camera_view image 생성
두 화면 크기 맞춤
좌우로 이어붙이기
최종 dashboard image 반환
```

---

## 11. 개발 단계 계획

## 11.1 1단계: 프로젝트 기본 구조 생성

목표:

```text
ss_depth ROS2 Python 패키지 생성
ros2 run으로 dashboard_node 실행 가능하게 만들기
```

완료 기준:

```text
ros2 run ss_depth dashboard_node 실행 시 빈 OpenCV 창 또는 로그 출력
```

---

## 11.2 2단계: RealSense 1인칭 화면 구현

목표:

```text
color image 표시
aligned depth image 수신
카메라 화면에 GO 화살표 표시
```

완료 기준:

```text
Jetson 모니터에서 카메라 화면과 GO 화살표가 표시됨
```

---

## 11.3 3단계: depth 장애물 bbox 구현

목표:

```text
0.4m 이내 가까운 픽셀 덩어리 검출
bbox 표시
bbox 거리 표시
```

완료 기준:

```text
카메라 앞 0.4m 이내 물체에 bbox와 거리 표시
작은 픽셀 노이즈는 bbox로 표시되지 않음
```

---

## 11.4 4단계: 5분할 방향 판단 구현

목표:

```text
하단 ROI 5분할
영역별 장애물 후보 계산
GO / TURN_LEFT / TURN_RIGHT 판단
카메라 화면에 회피 권장 방향 화살표 표시
```

완료 기준:

```text
장애물 없음 → GO
왼쪽 가까운 장애물 → TURN_RIGHT
오른쪽 가까운 장애물 → TURN_LEFT
중앙 가까운 장애물 → 더 안전한 방향 표시
```

---

## 11.5 5단계: 상태 안정화 구현

목표:

```text
3프레임 연속 같은 판단이 나왔을 때만 방향 상태 변경
```

완료 기준:

```text
화살표가 순간 노이즈로 과도하게 깜빡이지 않음
```

---

## 11.6 6단계: 지도 화면 기본 구현

목표:

```text
/map OccupancyGrid 수신
지도 이미지를 OpenCV 화면으로 렌더링
```

완료 기준:

```text
SLAM으로 생성된 지도가 대시보드 왼쪽 화면에 표시됨
```

---

## 11.7 7단계: 구루마 위치와 방향 표시

목표:

```text
pose 또는 tf 기반 현재 위치 수신
지도 위에 자동차 모양 아이콘 표시
현재 방향 표시
```

완료 기준:

```text
구루마를 움직이면 지도 위 아이콘 위치 또는 방향이 변경됨
```

---

## 11.8 8단계: 대시보드 화면 합성

목표:

```text
왼쪽: 지도 화면
오른쪽: 1인칭 카메라 화면
두 화면을 하나의 OpenCV 창으로 표시
```

완료 기준:

```text
하나의 모니터 화면에 지도와 카메라 화면이 동시에 표시됨
```

---

## 11.9 9단계: 목적지와 예상 경로 표시

목표:

```text
목적지 좌표 설정
지도 위에 목적지 표시
현재 위치에서 목적지까지 점선 경로 표시
```

초기 구현:

```text
직선 점선 경로
```

추후 구현:

```text
OccupancyGrid 기반 A* 경로
```

완료 기준:

```text
목적지와 예상 이동 동선이 지도 위에 표시됨
```

---

## 11.10 10단계: 지도 화면 회피 방향 표시

목표:

```text
depth 방향 판단 결과를 지도 화면에도 표시
```

완료 기준:

```text
카메라 화면과 지도 화면에 동일한 GO / TURN_LEFT / TURN_RIGHT 방향이 표시됨
```

---

## 12. 실행 구조

터미널 1: RealSense 실행

```bash
source /opt/ros/jazzy/setup.bash
source ~/ss_robot_ws/install/setup.bash

ros2 launch realsense2_camera rs_launch.py align_depth.enable:=true
```

터미널 2: LiDAR / SLAM 실행

```bash
source /opt/ros/jazzy/setup.bash
source ~/ss_robot_ws/install/setup.bash

ros2 launch slam_toolbox online_async_launch.py \
  slam_params_file:=/home/susoft/ss_robot_ws/config/lidar_only_slam.yaml
```

터미널 3: ss_depth 대시보드 실행

```bash
source /opt/ros/jazzy/setup.bash
source ~/ss_robot_ws/install/setup.bash
source ~/venvs/yolo_ros/bin/activate

ros2 run ss_depth dashboard_node
```

또는:

```bash
ros2 launch ss_depth dashboard.launch.py
```

---

## 13. Codex 개발 지시 기준

Codex 개발 시 아래 기준을 지킨다.

```text
1. ss_depth는 ROS2 ament_python 패키지로 만든다.
2. 실제 로봇 제어 코드는 넣지 않는다.
3. YOLO는 이번 단계에서 사용하지 않는다.
4. 먼저 RealSense 카메라 화면부터 안정화한다.
5. 그 다음 depth bbox와 방향 화살표를 구현한다.
6. 지도 화면은 SLAM 토픽이 준비된 뒤 붙인다.
7. 모든 파라미터는 config.py에 모은다.
8. depth 판단 로직과 화면 렌더링 로직을 분리한다.
9. 지도 렌더링 로직과 카메라 렌더링 로직을 분리한다.
10. 처음부터 복잡한 GUI 프레임워크를 쓰지 않는다.
11. 초기 화면은 OpenCV 하나의 창으로 만든다.
12. 코드가 길어지면 기능별 파일로 분리한다.
13. 테스트용 임시 파일을 프로젝트 루트에 흩뿌리지 않는다.
14. 각 단계가 실행 확인된 뒤 다음 단계로 넘어간다.
```

---

## 14. 이번 범위에서 제외할 항목

이번 개발 범위에서 제외할 항목은 다음과 같다.

```text
실제 모터 제어
RoboMaster 제어 연결
완전 자율주행
자동 목적지 추종
YOLO 객체 인식
웹 UI
복잡한 3D 시각화
음성 안내
클라우드 연동
```

이번 범위의 핵심은 다음이다.

```text
구루마 수동 이동 상태에서
LiDAR 지도와 RealSense 1인칭 화면을 동시에 보여주고
depth 기반 회피 권장 방향을 시각화하는 것
```

---

## 15. 최종 완료 기준

이번 개발이 완료되었다고 판단하는 기준은 다음과 같다.

```text
1. 모니터에 지도 화면과 1인칭 카메라 화면이 동시에 표시된다.
2. 지도 화면에는 현재 지도, 구루마 위치, 방향이 표시된다.
3. 1인칭 화면에는 전방 카메라 영상이 표시된다.
4. 1인칭 화면에는 0.4m 이내 가까운 장애물 bbox와 거리값이 표시된다.
5. 1인칭 화면에는 GO / TURN_LEFT / TURN_RIGHT 화살표가 표시된다.
6. 지도 화면에도 같은 회피 권장 방향이 표시된다.
7. 구루마를 직접 움직이면 지도와 카메라 화면의 정보가 실시간으로 갱신된다.
8. 목적지를 설정하면 지도 위에 예상 이동 동선이 점선으로 표시된다.
9. 실제 로봇 제어 없이도 사람이 화면을 보고 이동 방향을 판단할 수 있다.
```

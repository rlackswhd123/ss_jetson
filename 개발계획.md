# 목표점 기반 카메라 화살표 방향 표시 개발 계획

## 1. 개발 목표

현재 `CameraViewRenderer`는 `Direction` 값을 받아서 카메라 화면에 초록색 화살표를 표시한다.

현재 화살표 방향은 기존 장애물 회피 로직에 의해 결정된다.

```text
장애물 없음 → GO
왼쪽 장애물 → TURN_RIGHT
오른쪽 장애물 → TURN_LEFT
정면 장애물 → 좌/우 중 더 비어 보이는 쪽
```

이번 개발의 목표는 이 화살표 방향을 **장애물 분포 기준**이 아니라, **목표점으로 가기 위해 현재 로봇이 바라봐야 하는 방향 기준**으로 바꾸는 것이다.

즉, 화살표는 이제 단순 장애물 회피 방향이 아니라 다음 의미를 가져야 한다.

```text
현재 로봇 기준으로 목표점이 어느 방향에 있는가?
그리고 그 방향으로 가도 안전한가?
```

이번 단계에서는 실제 로봇을 움직이지 않는다.
`CameraViewRenderer`에 넘기는 `Direction` 값만 새 로직으로 바꾼다.

---

## 2. 이번 개발에서 하는 것

이번 작업에서 구현할 내용은 다음이다.

```text
1. 현재 로봇 pose와 목표점 goal을 기준으로 heading_error 계산
2. heading_error를 이용해서 목표점이 로봇 기준 왼쪽/정면/오른쪽 중 어디 있는지 판단
3. 장애물이 없으면 목표점 방향을 그대로 화살표로 표시
4. 목표점 방향에 장애물이 명확히 있으면 회피 방향을 화살표로 표시
5. 패널 같은 긴 장애물을 만났을 때 화살표가 좌우로 흔들리지 않도록 회피 방향을 일정 시간 고정
6. 기존 CameraViewRenderer는 최대한 수정하지 않고 그대로 사용
```

---

## 3. 이번 개발에서 하지 않는 것

이번 작업에서 아래 내용은 하지 않는다.

```text
- 실제 로봇 주행 명령 연결하지 않기
- linear_x, angular_z 기반 제어까지 만들지 않기
- Nav2 도입하지 않기
- A* 경로 탐색 구현하지 않기
- 지도 기반 최적 경로 생성하지 않기
- 로봇을 실제 목표점까지 이동시키는 기능 구현하지 않기
- CameraViewRenderer 구조를 크게 바꾸지 않기
```

이번 작업은 **화면 표시용 Direction 결정 로직 교체**가 목적이다.

---

## 4. 현재 코드에서 유지할 부분

현재 `CameraViewRenderer`는 다음 구조를 가진다.

```python
class CameraViewRenderer:
  def render(
    self,
    color_image: Any,
    obstacle_boxes: list[ObstacleBox],
    direction: Direction,
  ) -> Any:
    ...
```

이 파일은 이미 `Direction` 값에 따라 화살표를 잘 그리고 있다.

```text
Direction.GO         → 위쪽 화살표
Direction.TURN_LEFT  → 왼쪽 화살표
Direction.TURN_RIGHT → 오른쪽 화살표
```

따라서 `CameraViewRenderer`는 가능하면 수정하지 않는다.

바뀌어야 하는 것은 이 파일이 아니라, 이 파일에 전달되는 `direction` 값을 만드는 부분이다.

---

## 5. 기존 흐름

현재 흐름은 대략 다음과 같다.

```text
DepthObstacleDetector
→ section_analyses 생성
→ DirectionDecider.decide(section_analyses)
→ Direction 생성
→ CameraViewRenderer.render(..., direction)
→ 초록색 화살표 표시
```

이 구조의 문제는 `DirectionDecider`가 목표점 방향을 모른다는 것이다.

그래서 장애물이 없으면 항상 `GO`가 나온다.

하지만 새 로직에서는 장애물이 없더라도 목표점이 로봇 기준 왼쪽에 있으면 `TURN_LEFT`, 오른쪽에 있으면 `TURN_RIGHT`가 나와야 한다.

---

## 6. 변경 후 흐름

변경 후 흐름은 다음과 같아야 한다.

```text
DepthObstacleDetector
→ section_analyses 생성
→ 현재 pose와 goal로 heading_error 계산
→ GoalAwareDirectionDecider.decide(section_analyses, heading_error)
→ Direction 생성
→ CameraViewRenderer.render(..., direction)
→ 초록색 화살표 표시
```

즉, 기존 `DirectionDecider` 대신 새 목표점 기반 방향 결정기를 사용한다.

---

## 7. 새로 만들 클래스

새 클래스를 추가한다.

```python
class GoalAwareDirectionDecider:
    def decide(
        self,
        section_analyses: list[SectionAnalysis],
        heading_error: float | None,
    ) -> Direction:
        ...
```

역할:

```text
section_analyses와 heading_error를 보고 최종 Direction을 반환한다.
```

반환값은 반드시 기존 enum을 사용한다.

```python
Direction.GO
Direction.TURN_LEFT
Direction.TURN_RIGHT
```

이번 단계에서는 `VelocityCommand`, `linear_x`, `angular_z`를 만들 필요 없다.

---

## 8. heading_error 계산 방식

이 로직에서 가장 중요한 부분이다.

화살표는 지도 기준 절대 방향이 아니라, **현재 로봇 yaw 기준으로 목표점이 왼쪽에 있는지 오른쪽에 있는지**를 표시해야 한다.

따라서 반드시 아래 방식으로 계산한다.

```python
target_yaw = math.atan2(goal.y - pose.y, goal.x - pose.x)
heading_error = normalize_angle(target_yaw - pose.yaw)
```

`heading_error`는 반드시 `-pi ~ +pi` 범위로 정규화한다.

```python
def normalize_angle(angle: float) -> float:
    while angle > math.pi:
        angle -= 2.0 * math.pi
    while angle < -math.pi:
        angle += 2.0 * math.pi
    return angle
```

이 정규화가 빠지면 170도, 190도 같은 경계 상황에서 화살표 방향이 틀어진다.

---

## 9. heading_error 기준 방향 판단

초기 기준은 다음과 같이 둔다.

```text
heading_error가 0 근처 → 목표점이 정면
heading_error가 왼쪽 방향 → 목표점이 왼쪽
heading_error가 오른쪽 방향 → 목표점이 오른쪽
```

프로젝트 좌표계 기준으로 아래처럼 정의한다.

```text
heading_error < -HEADING_TOLERANCE_RAD → TURN_LEFT
heading_error >  HEADING_TOLERANCE_RAD → TURN_RIGHT
그 외 → GO
```

단, 실제 테스트에서 좌우가 반대로 나오면 부호를 한 곳에서만 보정한다.

예를 들어 로봇이 목표를 바라보다가 시계방향으로 80도 회전하면, 목표점은 로봇 기준 왼쪽에 있으므로 화살표는 `TURN_LEFT`가 나와야 한다.

```text
목표를 바라보는 상태
→ 로봇이 시계방향 80도 회전
→ 목표는 로봇 기준 왼쪽
→ Direction.TURN_LEFT
```

시계방향 170도도 동일하다.

```text
목표를 바라보는 상태
→ 로봇이 시계방향 170도 회전
→ 목표는 로봇 기준 왼쪽
→ Direction.TURN_LEFT
```

하지만 시계방향 190도는 최단 회전 기준으로 반대가 된다.

```text
목표를 바라보는 상태
→ 로봇이 시계방향 190도 회전
→ normalize_angle 결과는 반대 방향
→ Direction.TURN_RIGHT
```

이 동작이 반드시 맞아야 한다.

---

## 10. 장애물이 없을 때의 동작

장애물이 하나도 없으면 화살표는 목표점 방향만 보고 결정한다.

```text
목표점이 왼쪽 → TURN_LEFT
목표점이 정면 → GO
목표점이 오른쪽 → TURN_RIGHT
```

이 부분이 기존 로직과 가장 큰 차이다.

기존 로직:

```text
장애물 없음 → 무조건 GO
```

새 로직:

```text
장애물 없음 → 목표점이 있는 방향 표시
```

---

## 11. 장애물이 있을 때의 동작

장애물이 있을 때도 무조건 기존처럼 장애물 반대 방향으로 보내면 안 된다.

판단 우선순위는 다음과 같다.

```text
1. 목표점 방향을 우선한다.
2. 목표점 방향이 안전하면 목표점 방향 화살표를 표시한다.
3. 목표점 방향이 명확하게 막힌 경우에만 회피 방향 화살표를 표시한다.
4. 회피 방향은 너무 자주 바꾸지 않는다.
```

예시:

```text
목표점 왼쪽 + 왼쪽 안전 → TURN_LEFT
목표점 오른쪽 + 오른쪽 안전 → TURN_RIGHT
목표점 정면 + 정면 안전 → GO
```

목표 방향이 명확히 막혔을 때만 반대로 회피한다.

```text
목표점 왼쪽 + 왼쪽 명확히 막힘 → TURN_RIGHT
목표점 오른쪽 + 오른쪽 명확히 막힘 → TURN_LEFT
목표점 정면 + 정면 명확히 막힘 → 더 안전한 쪽
```

---

## 12. section_analyses 사용 방식

기존 `DepthObstacleDetector`가 반환하는 `section_analyses`를 그대로 사용한다.

예를 들어 `SECTION_COUNT = 5`라면 다음처럼 본다.

```text
0       1        2        3        4
왼쪽    왼쪽중앙   중앙     오른쪽중앙   오른쪽
```

기본 함수는 다음처럼 만든다.

```python
def get_blocked_indices(section_analyses):
    return {a.index for a in section_analyses if a.is_blocked}
```

```python
def is_left_blocked(section_analyses):
    center = config.SECTION_COUNT // 2
    return any(a.index < center and a.is_blocked for a in section_analyses)
```

```python
def is_right_blocked(section_analyses):
    center = config.SECTION_COUNT // 2
    return any(a.index > center and a.is_blocked for a in section_analyses)
```

```python
def is_front_blocked(section_analyses):
    center = config.SECTION_COUNT // 2
    return any(a.index == center and a.is_blocked for a in section_analyses)
```

필요하면 중앙 주변까지 front로 본다.

```text
front = center - 1, center, center + 1
```

단, 처음에는 너무 복잡하게 만들지 말고 기존 section 구조에 맞춰 단순하게 구현한다.

---

## 13. 목표 방향이 막혔는지 판단하는 방식

목표 방향을 먼저 `Direction`으로 바꾼다.

```text
heading_error 기준:
왼쪽 → target_direction = TURN_LEFT
정면 → target_direction = GO
오른쪽 → target_direction = TURN_RIGHT
```

그 다음 해당 방향이 막혔는지 확인한다.

```text
target_direction == TURN_LEFT
→ 왼쪽 영역이 명확히 막혔는지 확인

target_direction == GO
→ 정면 영역이 명확히 막혔는지 확인

target_direction == TURN_RIGHT
→ 오른쪽 영역이 명확히 막혔는지 확인
```

막히지 않았으면 목표 방향을 그대로 반환한다.

```text
목표 방향이 열려 있음 → target_direction 반환
```

막혔으면 회피 방향을 반환한다.

```text
목표 방향이 막힘 → avoid_direction 반환
```

---

## 14. 긴 장애물 처리

패널처럼 긴 장애물은 매 프레임 새로 좌우 판단을 하면 안 된다.

예를 들어 패널 앞에서 매 프레임 이렇게 바뀌면 실패다.

```text
TURN_LEFT
TURN_RIGHT
TURN_LEFT
TURN_RIGHT
```

긴 장애물을 처리하려면 방향 결정기에 상태가 있어야 한다.

최소 상태는 다음이다.

```python
class GoalArrowState(Enum):
    GO_TO_GOAL = "go_to_goal"
    AVOID_OBSTACLE = "avoid_obstacle"
```

상태 의미:

```text
GO_TO_GOAL
- 목표 방향을 우선 표시하는 상태

AVOID_OBSTACLE
- 목표 방향이 막혀서 한쪽으로 우회 중인 상태
- 한 번 정한 회피 방향을 유지하는 상태
```

---

## 15. 회피 상태 진입 조건

다음 조건이면 `AVOID_OBSTACLE` 상태로 들어간다.

```text
현재 상태가 GO_TO_GOAL이고,
목표 방향이 명확하게 막혀 있음
```

상태 진입 시 해야 할 일:

```text
1. 회피 방향 avoid_direction 결정
2. avoid_direction 저장
3. avoid 시작 시간 저장
```

예:

```python
self.state = GoalArrowState.AVOID_OBSTACLE
self.locked_avoid_direction = avoid_direction
self.avoid_started_at = now
```

---

## 16. 회피 방향 결정

회피 방향은 다음 기준으로 정한다.

```text
1. 목표 방향이 왼쪽인데 왼쪽이 막혔다면 오른쪽
2. 목표 방향이 오른쪽인데 오른쪽이 막혔다면 왼쪽
3. 목표 방향이 정면인데 정면이 막혔다면 더 덜 막힌 쪽
```

정면이 막힌 경우:

```text
왼쪽이 더 막힘 → TURN_RIGHT
오른쪽이 더 막힘 → TURN_LEFT
둘 다 비슷함 → 이전 회피 방향이 있으면 이전 방향 유지
```

목표는 **한 번 정한 회피 방향을 계속 유지하는 것**이다.

---

## 17. 회피 상태 유지 조건

`AVOID_OBSTACLE` 상태에서는 매 프레임 목표 방향으로 바로 돌아가면 안 된다.

아래 조건을 만족하기 전까지는 저장된 회피 방향을 계속 반환한다.

```text
1. 최소 회피 유지 시간이 지났음
2. 목표 방향이 다시 열려 있음
```

예:

```text
AVOID_MIN_HOLD_SECONDS = 1.0
```

즉, 회피 상태에 들어간 뒤 최소 1초 동안은 화살표 방향을 바꾸지 않는다.

---

## 18. 회피 상태 종료 조건

다음 조건을 만족하면 `GO_TO_GOAL` 상태로 돌아간다.

```text
최소 회피 유지 시간이 지났고,
목표 방향이 다시 막혀 있지 않음
```

상태 복귀 후에는 다시 heading_error 기준 목표 방향 화살표를 표시한다.

```text
AVOID_OBSTACLE
→ 목표 방향 다시 열림
→ GO_TO_GOAL
→ 목표 방향 화살표 표시
```

이번 단계에서는 실제 위치 변화나 목표 거리 감소까지는 필수로 넣지 않는다.
왜냐하면 지금 작업은 실제 로봇 이동이 아니라 화면 화살표 표시이기 때문이다.

다만 나중에 실제 주행까지 붙일 때는 목표 거리 감소 여부도 추가해야 한다.

---

## 19. 새 방향 결정기 의사코드

구현 방향은 다음과 같다.

```python
class GoalAwareDirectionDecider:
    def __init__(self):
        self.state = GoalArrowState.GO_TO_GOAL
        self.locked_avoid_direction: Direction | None = None
        self.avoid_started_at: float | None = None

    def decide(
        self,
        section_analyses: list[SectionAnalysis],
        heading_error: float | None,
    ) -> Direction:
        now = time.monotonic()

        if heading_error is None:
            self._reset_avoid_state()
            return self._fallback_direction(section_analyses)

        target_direction = self._direction_from_heading_error(heading_error)

        if self.state == GoalArrowState.AVOID_OBSTACLE:
            if self._can_return_to_goal(now, section_analyses, target_direction):
                self._reset_avoid_state()
                return target_direction

            if self.locked_avoid_direction is not None:
                return self.locked_avoid_direction

        if not self._is_direction_blocked(section_analyses, target_direction):
            return target_direction

        avoid_direction = self._choose_avoid_direction(
            section_analyses,
            target_direction,
        )

        self.state = GoalArrowState.AVOID_OBSTACLE
        self.locked_avoid_direction = avoid_direction
        self.avoid_started_at = now

        return avoid_direction
```

---

## 20. heading_error에서 Direction으로 변환

```python
def _direction_from_heading_error(self, heading_error: float) -> Direction:
    if heading_error < -config.HEADING_TOLERANCE_RAD:
        return Direction.TURN_LEFT

    if heading_error > config.HEADING_TOLERANCE_RAD:
        return Direction.TURN_RIGHT

    return Direction.GO
```

주의:

위 부호 기준은 프로젝트 좌표계에서 반드시 테스트한다.
만약 시계방향 80도 회전했는데 `TURN_RIGHT`가 나오면 좌우가 반대이므로 이 함수에서만 부호를 보정한다.

---

## 21. 방향이 막혔는지 판단

```python
def _is_direction_blocked(
    self,
    section_analyses: list[SectionAnalysis],
    direction: Direction,
) -> bool:
    if direction == Direction.GO:
        return self._is_front_blocked(section_analyses)

    if direction == Direction.TURN_LEFT:
        return self._is_left_blocked(section_analyses)

    if direction == Direction.TURN_RIGHT:
        return self._is_right_blocked(section_analyses)

    return False
```

---

## 22. fallback 동작

`heading_error`가 없다는 것은 목표점이 없거나 목표 방향을 계산할 수 없다는 뜻이다.

이 경우는 두 가지 중 하나로 처리한다.

권장 방식:

```text
heading_error is None
→ 기존 DirectionDecider 결과를 사용
```

즉, 목표점이 없을 때는 기존 장애물 회피 화살표를 그대로 유지할 수 있다.

단순 방식:

```text
heading_error is None
→ Direction.GO
```

프로젝트 기존 동작을 유지하려면 fallback으로 기존 `DirectionDecider`를 사용하는 것이 좋다.

---

## 23. config에 추가할 값

아래 값을 config에 추가한다.

```python
HEADING_TOLERANCE_RAD = 0.20
AVOID_MIN_HOLD_SECONDS = 1.0
```

의미:

```text
HEADING_TOLERANCE_RAD
- 목표점이 거의 정면이라고 판단할 각도 오차

AVOID_MIN_HOLD_SECONDS
- 긴 장애물 앞에서 화살표가 좌우로 흔들리지 않도록 회피 방향을 최소 유지하는 시간
```

필요하면 나중에 아래 값도 추가한다.

```python
USE_GOAL_AWARE_DIRECTION = True
```

기존 로직과 새 로직을 쉽게 전환하기 위한 플래그이다.

---

## 24. 실행 루프 변경

기존 코드 어딘가에 다음 흐름이 있을 것이다.

```python
obstacle_boxes, section_analyses = detector.detect(depth_image)
direction = direction_decider.decide(section_analyses)
camera_view = camera_view_renderer.render(
    color_image,
    obstacle_boxes,
    direction,
)
```

이 부분을 다음처럼 바꾼다.

```python
obstacle_boxes, section_analyses = detector.detect(depth_image)

heading_error = calculate_heading_error_if_goal_exists(
    current_pose,
    goal,
)

direction = goal_aware_direction_decider.decide(
    section_analyses,
    heading_error,
)

camera_view = camera_view_renderer.render(
    color_image,
    obstacle_boxes,
    direction,
)
```

`CameraViewRenderer` 호출부는 그대로 유지한다.

---

## 25. 목표점과 현재 pose가 아직 없을 경우

현재 프로젝트에 목표점 UI나 정확한 pose 계산이 아직 없다면, 처음에는 테스트용 입력으로 구현한다.

예:

```text
heading_error를 임시로 직접 넣어서 테스트
```

테스트 예:

```python
heading_error = math.radians(-80)
```

기대 결과:

```text
Direction.TURN_LEFT
```

```python
heading_error = math.radians(-170)
```

기대 결과:

```text
Direction.TURN_LEFT
```

```python
heading_error = math.radians(190)
heading_error = normalize_angle(heading_error)
```

기대 결과:

```text
Direction.TURN_RIGHT
```

단, 실제 부호 기준은 프로젝트 좌표계 테스트 후 맞춘다.

---

## 26. 테스트 항목

### 테스트 1: 장애물 없음 + 목표 정면

입력:

```text
heading_error = 0
장애물 없음
```

기대 결과:

```text
Direction.GO
```

---

### 테스트 2: 장애물 없음 + 목표 왼쪽

입력:

```text
heading_error = -80도
장애물 없음
```

기대 결과:

```text
Direction.TURN_LEFT
```

---

### 테스트 3: 장애물 없음 + 목표 오른쪽

입력:

```text
heading_error = +80도
장애물 없음
```

기대 결과:

```text
Direction.TURN_RIGHT
```

---

### 테스트 4: 시계방향 회전 80도

상황:

```text
처음 로봇이 목표를 바라보고 있음
로봇이 시계방향으로 80도 회전
```

기대 결과:

```text
목표는 로봇 기준 왼쪽
Direction.TURN_LEFT
```

---

### 테스트 5: 시계방향 회전 170도

상황:

```text
처음 로봇이 목표를 바라보고 있음
로봇이 시계방향으로 170도 회전
```

기대 결과:

```text
목표는 로봇 기준 왼쪽
Direction.TURN_LEFT
```

---

### 테스트 6: 시계방향 회전 190도

상황:

```text
처음 로봇이 목표를 바라보고 있음
로봇이 시계방향으로 190도 회전
```

기대 결과:

```text
최단 회전 기준 반대 방향
Direction.TURN_RIGHT
```

---

### 테스트 7: 목표 방향이 막힘

입력:

```text
목표점 왼쪽
왼쪽 section blocked
```

기대 결과:

```text
Direction.TURN_RIGHT
```

---

### 테스트 8: 긴 장애물 앞 화살표 흔들림 방지

입력:

```text
패널 같은 긴 장애물
section_analyses가 프레임마다 조금씩 흔들림
```

기대 결과:

```text
한 번 AVOID_OBSTACLE 상태에 들어가면
AVOID_MIN_HOLD_SECONDS 동안 같은 회피 방향 유지
TURN_LEFT/TURN_RIGHT가 매 프레임 바뀌지 않음
```

---

## 27. 완료 기준

이번 개발은 아래 조건을 만족하면 완료이다.

```text
1. CameraViewRenderer는 기존처럼 Direction을 받아 초록색 화살표를 그린다.
2. CameraViewRenderer 자체 구조는 거의 변경하지 않는다.
3. Direction 값은 새 GoalAwareDirectionDecider에서 생성된다.
4. 장애물이 없을 때 목표점이 왼쪽이면 TURN_LEFT가 나온다.
5. 장애물이 없을 때 목표점이 오른쪽이면 TURN_RIGHT가 나온다.
6. 장애물이 없을 때 목표점이 정면이면 GO가 나온다.
7. 로봇이 목표를 바라보다가 시계방향 80도 회전하면 TURN_LEFT가 나온다.
8. 로봇이 목표를 바라보다가 시계방향 170도 회전하면 TURN_LEFT가 나온다.
9. 로봇이 목표를 바라보다가 시계방향 190도 회전하면 TURN_RIGHT가 나온다.
10. 목표 방향이 명확히 막힌 경우에만 회피 방향이 나온다.
11. 긴 장애물 앞에서 화살표가 좌우로 계속 흔들리지 않는다.
12. 기존 obstacle box 표시는 그대로 유지된다.
13. 실제 로봇 이동 명령은 실행하지 않는다.
```

---

## 28. 한 문장 요약

이번 개발은 **초록색 화살표를 기존 장애물 회피 방향 표시에서, 현재 로봇 yaw 기준 목표점 방향과 장애물 상태를 함께 고려한 목표점 기반 방향 표시로 바꾸는 작업**이다.
